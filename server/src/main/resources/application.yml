spring:
  application:
    name: server

server:
  port: 8080

management:
  server:
    port: 28080
  endpoints:
    web:
      base-path: /management

eureka:
  instance:
    app-group-name: eureka
    metadata-map:
      environment: local
